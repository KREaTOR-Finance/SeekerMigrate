# SeekerMigrate Web (Internal Only)

This package is intentionally **not** a public product surface.

Purpose:
- Emulator/dev-client verification
- Internal flow checks before mobile releases

Policy:
- Primary surface is `apps/mobile` (Solana Mobile dApp Store)
- Keep web flow minimal (`/app/disclosure -> /app/wallet -> /app/identity -> /app/devkit`)
- Do not add marketing or public onboarding features here
